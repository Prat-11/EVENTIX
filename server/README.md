# Server Setup

## 1. Install Dependencies
```bash
cd server
npm install
```

## 2. Add Firebase Credentials
1. Place your `fireb-sdk.json` file in the server folder

## 3. Run Server
```bash
npm start
```

Server will run on http://localhost:5000
